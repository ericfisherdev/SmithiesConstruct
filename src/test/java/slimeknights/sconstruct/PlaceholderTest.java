package slimeknights.sconstruct;

import static org.junit.jupiter.api.Assertions.assertTrue;

import org.junit.jupiter.api.Test;

/**
 * Placeholder verifying that JUnit 5 is wired up and src/test/java is on the test source set.
 * Replace with real coverage as codecs and data-component round-trips are ported.
 */
class PlaceholderTest {

    @Test
    void junitPlatformIsWired() {
        assertTrue(true);
    }
}
